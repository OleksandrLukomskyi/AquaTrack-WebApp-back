# 🚀 Начните отсюда!

## ✅ Что уже сделано:

1. ✅ **Frontend настроен** - теперь использует локальный бэкенд (`http://localhost:3000`)
2. ✅ **JWT секреты сгенерированы** - готовы к использованию
3. ✅ **Шаблон .env создан** - нужно только заполнить MongoDB данные

## 📋 Что нужно сделать СЕЙЧАС:

### 1. Создать файл .env

**Вариант А (автоматически):**

```bash
node setup-env.js
```

**Вариант Б (вручную):**

- Скопируйте файл `ENV_TEMPLATE.txt` в `.env`
- Или создайте `.env` вручную с содержимым из `ENV_TEMPLATE.txt`

### 2. Заполнить данные MongoDB

Откройте файл `.env` и заполните:

```env
MONGODB_USER=ваш_username
MONGODB_PASSWORD=ваш_пароль
MONGODB_URL=cluster0.xxxxx.mongodb.net
MONGODB_DB=aquatrack
```

**Как получить данные MongoDB:**

#### Если используете MongoDB Atlas:

1. Зайдите на https://www.mongodb.com/cloud/atlas
2. Создайте бесплатный кластер (если еще нет)
3. Нажмите "Connect" → "Connect your application"
4. Скопируйте connection string, он выглядит так:
   ```
   mongodb+srv://USERNAME:PASSWORD@CLUSTER_URL/DATABASE_NAME?retryWrites=true&w=majority
   ```
5. Извлеките части:

   - `USERNAME` → `MONGODB_USER`
   - `PASSWORD` → `MONGODB_PASSWORD`
   - `CLUSTER_URL` (часть между @ и /) → `MONGODB_URL`
   - `DATABASE_NAME` → `MONGODB_DB`

6. **ВАЖНО:** Добавьте ваш IP в Network Access (whitelist)
   - Network Access → Add IP Address → Add Current IP Address
   - Или для разработки: `0.0.0.0/0` (разрешить все IP)

#### Если используете локальный MongoDB:

Измените файл `src/db/initMongoConnection.js` на:

```javascript
await mongoose.connect(`mongodb://localhost:27017/aquatrack`);
```

### 3. Установить зависимости (если еще не установлены)

**Backend:**

```bash
cd AquaTrack-WebApp-backend
npm install
```

**Frontend:**

```bash
cd AquaTrack-WebApp-frontend
npm install
```

### 4. Запустить проекты

**Терминал 1 - Backend:**

```bash
cd AquaTrack-WebApp-backend
npm run dev
```

Должно появиться: `Server is running on port 3000`

**Терминал 2 - Frontend:**

```bash
cd AquaTrack-WebApp-frontend
npm run dev
```

Должно появиться: `Local: http://localhost:4000`

### 5. Проверить работу

1. Откройте браузер: `http://localhost:4000`
2. Проверьте Swagger документацию: `http://localhost:3000/api-docs`
3. Попробуйте зарегистрировать нового пользователя

---

## 🎯 Минимальная конфигурация для работы

**Обязательно заполнить в .env:**

- ✅ `MONGODB_USER`
- ✅ `MONGODB_PASSWORD`
- ✅ `MONGODB_URL`
- ✅ `MONGODB_DB`

**Остальное уже настроено:**

- ✅ JWT секреты (ACCESS_SECRET, REFRESH_SECRET, JWT_SECRET)
- ✅ Порты и хосты
- ✅ Frontend настроен на локальный бэкенд

**Опционально (можно оставить пустым):**

- ⚪ Google OAuth
- ⚪ Cloudinary
- ⚪ SMTP (для email)

---

## 🆘 Проблемы?

### Ошибка подключения к MongoDB:

- Проверьте правильность данных в `.env`
- Убедитесь, что IP добавлен в whitelist MongoDB Atlas
- Проверьте интернет соединение

### Ошибка "Cannot find module":

- Выполните `npm install` в соответствующей папке

### Порт уже занят:

- Измените `PORT=3000` на другой порт в `.env`
- Или остановите процесс, использующий порт

### CORS ошибки:

- Убедитесь, что frontend запущен на `http://localhost:4000`
- Проверьте `FRONTEND_HOST=http://localhost:4000` в `.env`

---

## 📚 Дополнительная информация

- Подробная инструкция: `SETUP.md`
- Быстрый старт: `QUICK_START.md`
